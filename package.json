{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
     "start-api": "cd venv && venv/bin/flask run --no-debugger",
     "test-start": "next start && cd venv && flask run --no-debugger"
  
  },
  "dependencies": {
    "next": "10.0.5",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "proxy": "http://localhost:5000"
}
